{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": "lsof -i:1420 -sTCP:LISTEN -t >/dev/null || TAURI_DEV_HOST=$(ipconfig getifaddr en0) yarn dev",
    "beforeBuildCommand": "yarn run build",
    "frontendDist": "../dist"
  }
}
