# uremot end-to-end run — assets

Screenshots and the written report from the end-to-end test run of DecentPaste inside a
disposable [uremot](https://uremot.com) desktop instance.

This is an orphan branch. It carries **no source code** and is never merged — it exists so the
pull request can show images without adding ~1.3 MB of PNGs to the codebase.

See `REPORT.md` and `img/`.
