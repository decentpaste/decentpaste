{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (notifications, tray)",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["notification:default"]
}
