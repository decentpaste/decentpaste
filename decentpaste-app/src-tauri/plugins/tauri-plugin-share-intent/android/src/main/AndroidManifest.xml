<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.decentpaste.plugins.shareintent">

    <!--
        Activity alias that adds the SEND intent filter.
        Uses ${applicationId} placeholder which resolves to the app's package (com.decentpaste.application).
        The targetActivity points to the main app's MainActivity.
    -->
    <application>
        <activity-alias
            android:name="${applicationId}.ShareReceiverAlias"
            android:targetActivity="${applicationId}.MainActivity"
            android:exported="true"
            android:label="DecentPaste">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>
    </application>

</manifest>
